# Templates — canonical copies live at the repo root

See `../references/file-map.md`. All output templates are maintained
in one place: `templates/` at the repository root (campaign-brief.yaml,
artifact-manifest.yaml, state.json, claim-ledger.csv,
audience-message-matrix.md, launch-calendar.md, final-report.md).
This directory holds no copies to prevent drift.
